[build]
buildCommand = "npm install && npm run build:shared && npm run build --workspace=apps/server"

[deploy]
startCommand = "node apps/server/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
